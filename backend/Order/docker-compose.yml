services:
  order-backend:
    build:
      context: ..
      dockerfile: Order/Dockerfile
    ports:
      - "8001:8001"
    environment:
      APP_PORT: 8001
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
