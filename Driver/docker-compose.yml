services:
  driver-backend:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - .:/app
      - driver-uploads:/data/uploads
    restart: unless-stopped

volumes:
  driver-uploads:
